services:
  - type: web
    name: municipal-code-reviewer
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --timeout 180 --workers 1
    envVars:
      - key: ANTHROPIC_API_KEY
        sync: false   # You will paste this in the Render dashboard — never commit it to GitHub
